[package]
name = "comet-config"
version = "0.1.0"
edition = "2024"
description = "Configuration management for Comet Terminal"

[dependencies]
directories = "5.0"
toml = "0.8"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
notify = "7"

[dev-dependencies]
tempfile = "3.10"