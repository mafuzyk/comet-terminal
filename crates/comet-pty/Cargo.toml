[package]
name = "comet-pty"
version = "0.1.0"
edition = "2024"

[dependencies]
comet-core = { path = "../comet-core" }
portable-pty = "0.9"
tokio = { version = "1", features = ["rt-multi-thread", "io-std", "process"] }
vte = "0.11"
anyhow = "1.0"
thiserror = "1.0"